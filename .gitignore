# Сборка для магазинов — собирается из исходников скриптом tools/build.sh
build/

# Локальные настройки Claude Code: у каждого свои, к расширению не относятся
.claude/settings.local.json

# Служебные файлы macOS
.DS_Store
